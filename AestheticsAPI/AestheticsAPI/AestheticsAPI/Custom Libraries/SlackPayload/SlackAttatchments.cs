using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

/*
namespace AestheticsAPI.Custom_Libraries.SlackPayload
{
    public class SlackAttatchments : SlackMessage
    {
        public string title { get; set; }
        public string pretext { get; set; }
        public string[] mrkdwn_in = { "text", "pretext" };

        public SlackAttatchments(string txt, string un, bool md)
        {
            text = txt;
            username = un;
            mrkdwn = md;

            cleanPayload();
        }

        //extend the cleanPayload() method to the new data types
        public new void cleanPayload()
        {
            this.cleanPayload();

            title = cleanText(title);
            pretext = cleanText(pretext);
        }

        //to-do
        //set attatchment markdown properties
    }

    
}
*/